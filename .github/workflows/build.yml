name: Build Sources

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Install AS source dependencies
        run: |
          for src in ./src/as/*/; do
            [ -f "$src/res/source.json" ] || continue
            grep '"deprecated"' "$src/res/source.json" | grep -q 'true' && continue
            [ -f "$src/package.json" ] && (cd "$src" && npm ci)
          done

      - name: Setup Rust nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install aidoku-cli
        run: |
          mkdir -p $HOME/.local/bin
          curl -fsSL https://github.com/Aidoku/aidoku-cli/releases/download/v0.5.9/aidoku-cli_0.5.9_linux_x86_64.tar.gz | tar xz -C $HOME/.local/bin
          chmod +x $HOME/.local/bin/aidoku
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Build all sources
        run: |
          chmod +x src/rust/*/build.sh
          bash -O globstar ./build.sh

      - name: Commit and push compiled sources
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/
          git diff --staged --quiet || git commit -m "build: update compiled sources"
          git push
